# 로컬 테스트 체크리스트

## 사전 준비

### 1. 데이터베이스 마이그레이션
Supabase SQL Editor에서 실행:
```sql
-- players 테이블에 created_at 추가
ALTER TABLE players 
ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ DEFAULT NOW();

UPDATE players 
SET created_at = NOW() 
WHERE created_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_players_room_created ON players(room_id, created_at);

-- 기존 데이터 정리 (선택사항)
DELETE FROM rooms WHERE status = 'waiting';
DELETE FROM games WHERE status != 'finished';
```

### 2. 환경 변수 확인
```bash
# backend/.env
DATABASE_URL=postgresql://...
PORT=3000

# frontend/.env (또는 .env.local)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
```

### 3. 서버 시작
```bash
# 터미널 1: 백엔드
cd backend
npm run dev

# 터미널 2: 프론트엔드
cd frontend
npm run dev
```

## 테스트 시나리오

### ✅ 시나리오 1: 방 생성 및 참가 (예상 시간: 1분)
1. 브라우저 1에서 `localhost:3000` 접속
2. 닉네임 입력 후 "방 만들기" 클릭
3. **체크포인트**: 방 코드가 표시되는가?
4. **체크포인트**: 1번 슬롯에 방장 이름이 표시되는가?
5. 브라우저 2 (시크릿 모드)에서 `localhost:3000` 접속
6. 닉네임 입력 후 방 코드 입력하여 "참가하기" 클릭
7. **체크포인트**: 2번 슬롯에 참가자 이름이 표시되는가?
8. **체크포인트**: 양쪽 브라우저에서 모두 2명이 보이는가?

**예상 결과**:
```
슬롯 1: 👑 플레이어A (방장)
슬롯 2: 👤 플레이어B
슬롯 3: ➕ 빈 슬롯
슬롯 4: ➕ 빈 슬롯
슬롯 5: ➕ 빈 슬롯
```

**에러 체크**:
- [ ] 404 에러 → 백엔드 서버 확인
- [ ] WebSocket 에러 → CORS 설정 확인
- [ ] "column p.created_at does not exist" → 마이그레이션 실행 필요

---

### ✅ 시나리오 2: AI 추가 (예상 시간: 30초)
1. 방장 브라우저에서 3번 슬롯의 ⚙️ 버튼 클릭
2. "🤖 AI 추가" 선택
3. **체크포인트**: 3번 슬롯에 AI 이름이 표시되는가?
4. **체크포인트**: 참가자 브라우저에서도 AI가 보이는가?

**예상 결과**:
```
슬롯 1: 👑 플레이어A (방장)
슬롯 2: 👤 플레이어B
슬롯 3: 🤖 똑똑한로봇42
슬롯 4: ➕ 빈 슬롯
슬롯 5: ➕ 빈 슬롯
```

**에러 체크**:
- [ ] 400 에러 → 슬롯 업데이트 로직 확인
- [ ] AI가 추가되지 않음 → RoomService.updateSlot 확인

---

### ✅ 시나리오 3: 게임 시작 (예상 시간: 1분)
1. 방장 브라우저에서 "게임 시작" 버튼 클릭
2. **체크포인트**: 모든 플레이어가 게임 화면으로 이동하는가?
3. **체크포인트**: 우측 상단에 여행지 카드가 표시되는가?
4. **체크포인트**: 왼쪽 패널에 플레이어 목록이 표시되는가?
5. **체크포인트**: 현재 턴 플레이어가 강조 표시되는가?
6. **체크포인트**: "Day 1 / 14"가 표시되는가?

**예상 결과**:
```
헤더:
- 🌙 열네 밤의 꿈
- Day 1 / 14
- 🎯 내 여행지: 제주 (자연 ×3, 맛 ×2, 물놀이 ×1)

플레이어 목록:
- 🎯 플레이어A (나) #1 [현재 턴 - 노란색 강조]
- 플레이어B #2
- 똑똑한로봇42 #3

메시지:
- "게임을 시작합니다!"
- 🎯 이동 필요
```

**에러 체크**:
- [ ] 게임 화면으로 이동하지 않음 → WebSocket 연결 확인
- [ ] 여행지 카드가 표시되지 않음 → GameSetupService 확인
- [ ] 턴 플레이어가 표시되지 않음 → current_turn_player_id 확인

---

### ✅ 시나리오 4: 첫 턴 진행 (예상 시간: 1분)
1. 선 플레이어 브라우저에서 게임 보드 확인
2. **체크포인트**: 인접한 칸(2번)이 밝게 표시되는가?
3. **체크포인트**: 애니메이션 효과가 있는가?
4. 2번 칸 클릭
5. **체크포인트**: "2번 칸으로 이동했습니다" 메시지가 표시되는가?
6. **체크포인트**: 0.5초 후 자동으로 행동이 수행되는가?
7. **체크포인트**: 일반 계획 카드를 획득했다는 메시지가 표시되는가?
8. **체크포인트**: 1.5초 후 자동으로 턴이 종료되는가?
9. **체크포인트**: 다음 플레이어에게 턴이 넘어가는가?

**예상 결과**:
```
1. 이동 전:
   - 현재 위치: 1번
   - 인접 칸: 2번 (밝게 표시, 펄스 애니메이션)

2. 이동 후:
   - 현재 위치: 2번
   - 메시지: "2번 칸으로 이동했습니다. 행동을 수행합니다..."

3. 행동 후:
   - 메시지: "🎴 [카드 이름] 카드를 획득했습니다!"
   - 손패에 카드 추가

4. 턴 종료 후:
   - 메시지: "턴이 종료되었습니다."
   - 다음 플레이어 강조 표시
```

**에러 체크**:
- [ ] 인접 칸이 표시되지 않음 → GameBoard 로직 확인
- [ ] 클릭해도 이동하지 않음 → handleMove 함수 확인
- [ ] 행동이 수행되지 않음 → TurnService.performAction 확인
- [ ] 턴이 종료되지 않음 → TurnManager.endTurn 확인
- [ ] 다음 플레이어로 넘어가지 않음 → WebSocket 이벤트 확인

---

### ✅ 시나리오 5: AI 플레이어 자동 턴 (예상 시간: 10초)
1. AI 플레이어의 턴이 되면 대기
2. **체크포인트**: 5초 이내에 AI가 자동으로 이동하는가?
3. **체크포인트**: AI가 자동으로 행동을 수행하는가?
4. **체크포인트**: AI의 턴이 자동으로 종료되는가?

**예상 결과**:
```
AI 턴 시작:
- 메시지: "⏳ 똑똑한로봇42의 턴"
- 5초 이내에 자동 진행

AI 턴 종료:
- 다음 플레이어로 턴 넘김
```

**에러 체크**:
- [ ] AI가 턴을 진행하지 않음 → AIScheduler 확인
- [ ] AI가 30초 이상 대기 → AIPlayerService 로직 확인

---

### ✅ 시나리오 6: 여러 턴 진행 (예상 시간: 5분)
1. 각 플레이어가 3턴씩 진행
2. **체크포인트**: 턴 순서가 올바른가?
3. **체크포인트**: 이동 제약이 작동하는가? (직전 칸 불가)
4. **체크포인트**: TC가 올바르게 증감하는가?
5. **체크포인트**: 손패에 카드가 추가되는가?

**예상 결과**:
```
턴 순서:
Day 1: 플레이어A → 플레이어B → AI → 플레이어A → ...
Day 2: 플레이어B → AI → 플레이어A → 플레이어B → ...
```

**에러 체크**:
- [ ] 턴 순서가 뒤섞임 → turn_order 확인
- [ ] 이동 제약이 작동하지 않음 → last_position 확인
- [ ] TC가 업데이트되지 않음 → applyMoneyEffect 확인

---

## 에러 로그 수집

### 백엔드 로그 확인
```bash
# 터미널에서 에러 메시지 확인
# 특히 다음 패턴 주의:
- "column ... does not exist"
- "relation ... does not exist"
- "null value in column"
- "violates foreign key constraint"
```

### 프론트엔드 로그 확인
```javascript
// 브라우저 콘솔 (F12) 확인
// 특히 다음 패턴 주의:
- "Failed to load resource: 404"
- "WebSocket connection failed"
- "Cannot read property ... of undefined"
- "Unexpected token"
```

### 네트워크 로그 확인
```
브라우저 F12 → Network 탭
- API 요청 실패 (빨간색)
- WebSocket 연결 상태
- 응답 시간 (30초 이상 주의)
```

## 성공 기준

### 필수 기능
- [x] 방 생성 및 참가
- [x] 슬롯 시스템 (방장 1번, 참가자 순서대로)
- [x] AI 추가
- [x] 게임 시작
- [x] 여행지 카드 표시
- [x] 턴 진행 (이동 → 행동 → 턴 종료)
- [x] AI 자동 턴
- [x] 턴 순서 관리

### 선택 기능
- [ ] 결심 토큰 사용
- [ ] 찬스 카드 효과
- [ ] 공동 목표 기여
- [ ] 14일차 종료 및 결산
- [ ] 최종 점수 계산

## 다음 단계

테스트 완료 후:
1. 발견된 버그 목록 작성
2. 우선순위 설정
3. 버그 수정
4. 재테스트
